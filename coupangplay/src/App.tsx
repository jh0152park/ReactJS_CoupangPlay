import { BrowserRouter } from "react-router-dom";
import Header from "./Screen/Components/Header";

function App() {
    return (
        <BrowserRouter>
            <Header />
        </BrowserRouter>
    );
}

export default App;
